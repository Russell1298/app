# Copy of .env.example — real credentials, never commit this file.

# PostgreSQL
POSTGRES_USER=scanner
POSTGRES_PASSWORD=Sc4nn3r_S3cur3!2024
POSTGRES_DB=scanner

# Constructed automatically by docker-compose from the vars above.
# Set this manually when running the API outside Docker.
# DATABASE_URL=postgresql+asyncpg://scanner:Sc4nn3r_S3cur3!2024@localhost:5432/scanner
